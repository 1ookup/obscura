// Compatibility placeholder.
//
// Event interfaces are defined one-per-file under env/events. Shared private
// state and WebIDL helpers moved to support/state.js; keep this path empty for
// downstream manifests that still reference the historical module name.
