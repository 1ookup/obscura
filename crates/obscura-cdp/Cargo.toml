[package]
name = "obscura-cdp"
version.workspace = true
edition.workspace = true

[features]
default = ["stealth"]
# Pure forwarding, mirroring `render` below: obscura-cdp has no stealth-gated
# code of its own, but without this `cargo build -p obscura-cdp` would silently
# link a non-stealth browser/js/net while every other entry point gets stealth.
stealth = ["obscura-browser/stealth", "obscura-js/stealth", "obscura-net/stealth"]
# Enable Page.captureScreenshot via obscura-render. Propagates render to
# obscura-browser so Page::screenshot is available.
render = ["obscura-browser/render", "dep:image", "dep:png"]

[dependencies]
obscura-dom = { workspace = true }
obscura-browser = { workspace = true }
obscura-js = { workspace = true }
tokio = { workspace = true }
tokio-tungstenite = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
url = { workspace = true }
uuid = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
base64 = { workspace = true }
futures-util = "0.3"
obscura-net = { workspace = true }
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "webp"], optional = true }
png = { version = "0.18", optional = true }

[target.'cfg(target_env = "gnu")'.dependencies]
libc = "0.2"

[dev-dependencies]
tokio-tungstenite = { workspace = true }
futures-util = "0.3"
